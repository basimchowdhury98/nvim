{
    "$schema": "https://opencode.ai/config.json",
    "compaction": {
        "auto": false,
        "prune": true,
        "reserved": 10000
    },
    "default_agent": "plan",
    "agent": {
        "plan": {
            "color": "#12a61c"
        },
        "build": {
            "color": "#bf3d55"
        }
    },
    "mcp": {
        "playwright": {
            "type": "local",
            // Chromium (requires: npx playwright install chromium)
            "command": ["npx", "@playwright/mcp@latest", "--caps", "vision"],
            // Chrome (uses system-installed Chrome, no download needed)
            // "command": ["npx", "@playwright/mcp@latest", "--browser", "chrome", "--caps", "vision"],
            "enabled": true
        }
    },
    "command": {
        "build" : {
            "template": "Build. $ARGUMENTS",
            "description": "Run build without switching to build agent",
            "agent": "build"
        }
    }
}

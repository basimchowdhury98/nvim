This is my personal neovim config. It houses my configuration, imported plugin configs(in ./lua/plugins/), and also custom configs/utils(in ./lua/utils/).
This config is used in multiple PCs (work, personal, etc). It used to need to be cross platform because I used windows, mac, and linux but now its only 
used with linux.
